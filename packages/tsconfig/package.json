{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "tsconfig",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "fix:formatting": "../../node_modules/.bin/prettier --write .",
    "lint:formatting": "../../node_modules/.bin/prettier --check .",
    "fix": "yarn fix:formatting",
    "lint": "yarn lint:formatting"
  }
}
